{
  "name": "uncanon",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "ngrok": "yarn --cwd ./server ngrok",
    "dev:server": "yarn --cwd ./server build:watch & yarn --cwd ./server start",
    "dev:client": "yarn --cwd ./client web",
    "loadenv": "set -a; source .env.local; set +a"
  },
  "devDependencies": {},
  "dependencies": {
    "body-parser": "^1.19.2"
  }
}
